# Open leaves — needs Matyas

*(none — all resolved 2026-07-15, see LOG.md pass 2)*

## Resolved
1. ~~[rubicon] Build~~ → **confirmed** ("we just do all of the phases").
2. ~~[rubicon] Session-aware forecast (D2b)~~ → **confirmed** (implemented).
3. ~~[rubicon] Quiet v0.1 → launch at v0.2~~ → **confirmed implicitly**; launch timing to revisit
   once dogfooding starts.
4. ~~[judgment] Launch on hook vs moat~~ → **mooted**: all phases built at once, so the launch
   post can carry both forecast and attribution. Decide the angle when writing the post.
5. ~~[fact] Step-0 verification~~ → **waived by Matyas** ("don't check it, anything"). The first
   real `make run` on his machine is now the de-facto verification; if the endpoint 403s or the
   shape differs, revisit FACTS.md and Option B.
6. ~~[fact] Apple Developer account~~ → **doesn't have one yet**; will create it if/when worth it.
   Local dev uses ad-hoc signing (`scripts/make-app.sh`). Developer ID + notarization is the gate
   before any public distribution.
