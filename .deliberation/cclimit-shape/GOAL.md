# Goal

**Decision:** Settle the four open shape calls for CClimit so development can start:
(1) build vs install-Claude-God, (2) forecast design, (3) launch sequencing, (4) fleet-attribution timing.

**Unblocks:** shape decisions → dev plan → v0.1 build → v0.2 forecast → public launch → portfolio/job-search asset.

**Larger goal (inferred from docs/product.md):** daily utility + portfolio piece backing the
agentic-tooling job-search story; NOT revenue (free/MIT is settled).

**Hard constraints (from docs/product.md + brainstorm pass 2026-07-15):**
- v0.1 timeboxed to ~one focused weekend; native Swift/SwiftUI, no Electron.
- Read-only credential access; zero telemetry; signed + notarized from v0.1.
- Conservative polling (60–120 s active / 5–10 min idle, backoff on 429) — non-negotiable.
- One-shot launch channels (r/ClaudeAI, HN): must not be spent on a non-differentiated post.
- Peak-hours badge: cut (brainstorm).
- Utilization sample persistence: starts in v0.1 (brainstorm).

**Phase:** actional
**Rubicon:** conservative
**resurface_when:** Matyas next touches ~/dev/cclimit or mentions CClimit.
